generator client {
  provider = "prisma-client-js"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Problems {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  constraints String[]
  examples    String
  starterCode Json
}
