generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Problems { 
  id    Int     @id @default(autoincrement()) 
  title String
  description String
  constraints String[]
  examples String
  starterCode Json
}