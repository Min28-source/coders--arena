generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Problems { 
  id    Int     @id @default(autoincrement()) 
  title String
  description String
  constraints String[]
  examples Json
}